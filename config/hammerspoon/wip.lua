-- TODO: finish

-- req("sabatical")
-- req("mini")
-- require("spoons")
local M = {}

function M.bowser()
  -- local success, obj, output = hs.osascript.javascript([[
  --   (function() {
  --     var browser = Application('Brave Browser Nightly');
  --    // browser.activate();
  --     return browser.windows[0].activeTab();
  --   })();
  --   ]])

  -- print(I({ success, obj, output }))
  -- print(M.getSelectedText())
end

return M
