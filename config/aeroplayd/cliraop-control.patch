diff --git a/src/cliraop.c b/src/cliraop.c
index 74b1791..7baaa45 100644
--- a/src/cliraop.c
+++ b/src/cliraop.c
@@ -25,0 +26 @@
+#include <errno.h>
@@ -74,0 +76 @@ static int print_usage(char *argv[])
+			   "\t[-c <volume control FIFO>]\n"
@@ -178 +180,2 @@ int main(int argc, char *argv[]) {
-	char *secret = NULL, *md = NULL, *et = NULL;
+	char *secret = NULL, *md = NULL, *et = NULL, *control = NULL;
+	int control_fd = -1;
@@ -195,0 +199,3 @@ int main(int argc, char *argv[]) {
+		} else if (!strcmp(argv[i],"-c")) {
+			if (++i >= argc) return print_usage(argv);
+			control=argv[i];
@@ -265,0 +272,15 @@ int main(int argc, char *argv[]) {
+#if !WIN
+	if (control) {
+		int flags;
+
+		control_fd = open(control, O_RDONLY | O_NONBLOCK);
+		flags = fcntl(infile, F_GETFL);
+		if (control_fd == -1 || flags == -1 || fcntl(infile, F_SETFL, flags | O_NONBLOCK) == -1) {
+			LOG_ERROR("cannot configure volume control FIFO %s", control);
+			if (control_fd != -1) close(control_fd);
+			close_platform(interactive);
+			exit(1);
+		}
+	}
+#endif
+
@@ -332,0 +354,21 @@ int main(int argc, char *argv[]) {
+#if !WIN
+		if (control_fd != -1) {
+			uint8_t value;
+			ssize_t count;
+
+			while ((count = read(control_fd, &value, 1)) == 1) {
+				if (value <= 100 && !raopcl_set_volume(raopcl, raopcl_float_volume(value))) {
+					LOG_ERROR("cannot set volume to %u", value);
+					goto done;
+				}
+			}
+			if (count == 0) {
+				close(control_fd);
+				control_fd = -1;
+			} else if (count < 0 && errno != EAGAIN && errno != EWOULDBLOCK && errno != EINTR) {
+				LOG_ERROR("cannot read volume control FIFO");
+				goto done;
+			}
+		}
+#endif
+
@@ -334,0 +377,8 @@ int main(int argc, char *argv[]) {
+			if (n < 0) {
+				if (errno == EAGAIN || errno == EWOULDBLOCK || errno == EINTR) {
+					usleep(2000);
+					continue;
+				}
+				LOG_ERROR("cannot read audio input");
+				goto done;
+			}
@@ -368,5 +418 @@ int main(int argc, char *argv[]) {
-				raopcl_disconnect(raopcl);
-				raopcl_destroy(raopcl);
-				free(buf);
-				close_platform(interactive);
-				exit(0);
+				goto done;
@@ -381,0 +428 @@ int main(int argc, char *argv[]) {
+done:
@@ -385,0 +433 @@ exit:
+	if (control_fd != -1) close(control_fd);
