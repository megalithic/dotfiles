# POSIX login profile for mise-managed dotfiles.
# Keep minimal: interactive bash/zsh hooks live in their rc files.
if [ -d "$HOME/.local/bin" ]; then
  PATH="$HOME/.local/bin:$PATH"
fi
if [ -d "$HOME/bin" ]; then
  PATH="$HOME/bin:$PATH"
fi
export PATH
