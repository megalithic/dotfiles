# Committed static fragment linked to ~/.local/share/tmux/nix.conf by the
# mise [dotfiles] table. Replaces the Home Manager-generated file of the same
# name (tmux.conf sources it at startup).
set -g default-shell "/opt/homebrew/bin/fish"
