#!/usr/bin/env zsh
#
# Run all platform-specific installers.

# find macos installers and run them interactively
echo ""
echo ":: ------ running installers for $PLATFORM..."
echo ""

find $DOTS -name $PLATFORM.sh | while read installer ; do echo "-- running installer: $installer" ; sh "${installer}" > /dev/null ; done
