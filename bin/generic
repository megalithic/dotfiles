#!/bin/zsh

# find generic installers and run them interactively
echo ""
echo ":: ------ running generic installers..."
echo ""

find $DOTS -name all.sh | while read installer ; do echo "-- running installer: $installer"; sh -c "${installer}" > /dev/null ; done
