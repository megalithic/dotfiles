_: {
  # Ollama is intentionally not installed by default. Local inference is served
  # by home/common/programs/llama-cpp-local/ instead, and current nixpkgs
  # ollama builds require Xcode's Metal toolchain on Darwin.
}
